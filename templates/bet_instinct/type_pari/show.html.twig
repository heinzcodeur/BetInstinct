{{include('head.html.twig')}}


{% block body %}
    <h1>TypePari</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_pari.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_pari.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_pari.description }}</td>
            </tr>
            <tr>
                <th>Cotes</th>
                <td>{{ type_pari.cotes ? type_pari.cotes|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bet_instinct_type_pari_index') }}">back to list</a>

    <a href="{{ path('bet_instinct_type_pari_edit', {'id': type_pari.id}) }}">edit</a>

    {{ include('bet_instinct/type_pari/_delete_form.html.twig') }}

    {{include('end.html.twig')}}

{% endblock %}
