{{include('head.html.twig')}}

{% block body %}
    <h1>Classement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classement.id }}</td>
            </tr>
            <tr>
                <th>Association</th>
                <td>{{ classement.association }}</td>
            </tr>
            <tr>
                <th>Ranking</th>
                <td>{{ classement.ranking }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bet_instinct_classement_index') }}">back to list</a>

    <a href="{{ path('bet_instinct_classement_edit', {'id': classement.id}) }}">edit</a>

    {{ include('bet_instinct/classement/_delete_form.html.twig') }}

    {{include('end.html.twig')}}

{% endblock %}
