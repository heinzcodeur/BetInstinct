{{include('head.html.twig')}}


{% block body %}
    <h1>Create new Classement</h1>

    {% for d in app.flashes('danger') %}
    <div class="alert alert-danger">{{ d }}</div>
    {% endfor %}

    {{ include('bet_instinct/classement/_form.html.twig') }}


    <a href="{{ path('bet_instinct_classement_index') }}" class="btn btn-primary">retour</a>
    </div>


    {{include('end.html.twig')}}

{% endblock %}
