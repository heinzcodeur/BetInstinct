{{include('head.html.twig')}}

{% block title %}Continent index{% endblock %}

{% block body %}
    <h1>Continent index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for continent in continents %}
            <tr>
                <td>{{ continent.id }}</td>
                <td>{{ continent.name }}</td>
                <td>
                    <a href="{{ path('bet_instinct_continent_show', {'id': continent.id}) }}">show</a>
                    <a href="{{ path('bet_instinct_continent_edit', {'id': continent.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('bet_instinct_continent_new') }}">Create new</a>
{% endblock %}
