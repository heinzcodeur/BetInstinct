{% extends 'base.html.twig' %}

{% block title %}Continent{% endblock %}

{% block body %}
    <h1>Continent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ continent.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ continent.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bet_instinct_continent_index') }}">back to list</a>

    <a href="{{ path('bet_instinct_continent_edit', {'id': continent.id}) }}">edit</a>

    {{ include('bet_instinct/continent/_delete_form.html.twig') }}
{% endblock %}
