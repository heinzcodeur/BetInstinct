{% extends 'base.html.twig' %}

{% block title %}Document{% endblock %}

{% block body %}
    <h1>Document</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document.id }}</td>
            </tr>
            <tr>
                <th>Nom_document</th>
                <td>{{ document.nomDocument }}</td>
            </tr>
            <tr>
                <th>File</th>
                <td>{{ document.file }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ document.filename }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bet_instinct_document_index') }}">back to list</a>

    <a href="{{ path('bet_instinct_document_edit', {'id': document.id}) }}">edit</a>

    {{ include('bet_instinct/document/_delete_form.html.twig') }}
{% endblock %}
