{% extends 'base.html.twig' %}

{% block title %}Choix{% endblock %}

{% block body %}
    <h1>Choix</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ choix.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ choix.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bet_instinct_choix_index') }}">back to list</a>

    <a href="{{ path('bet_instinct_choix_edit', {'id': choix.id}) }}">edit</a>

    {{ include('bet_instinct/choix/_delete_form.html.twig') }}
{% endblock %}
