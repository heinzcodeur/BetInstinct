{{ include('head.html.twig')}}

{% block body %}
    <h1>Association</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>2 55 03 99 326 627
                </th>
                <td>{{ association.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ association.name }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('bet_instinct_association_index') }}">back to list</a>

    <a href="{{ path('bet_instinct_association_edit', {'id': association.id}) }}">edit</a>

    {{ include('bet_instinct/association/_delete_form.html.twig') }}
{% endblock %}
