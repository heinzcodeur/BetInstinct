{{include('head.html.twig')}}

{% block body %}
    <h1>Equipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ equipe.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bet_instinct_equipe_index') }}">back to list</a>

    <a href="{{ path('bet_instinct_equipe_edit', {'id': equipe.id}) }}">edit</a>

{% endblock %}
