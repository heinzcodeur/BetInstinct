<form method="post" action="{{ path('bet_instinct_equipe_delete', {'id': equipe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');"class="inline" >
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipe.id) }}">
    <button class="btn btn-outline-danger">Delete</button>
</form>
