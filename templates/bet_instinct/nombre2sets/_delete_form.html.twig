<form method="post" action="{{ path('bet_instinct_nombre2sets_delete', {'id': nombre2set.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nombre2set.id) }}">
    <button class="btn">Delete</button>
</form>
