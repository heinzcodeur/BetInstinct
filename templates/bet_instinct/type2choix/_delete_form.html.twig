<form method="post" action="{{ path('bet_instinct_type2choix_delete', {'id': type2choix.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type2choix.id) }}">
    <button class="btn">Delete</button>
</form>
