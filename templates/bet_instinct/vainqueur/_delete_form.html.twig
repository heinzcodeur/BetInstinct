<form method="post" action="{{ path('bet_instinct_vainqueur_delete', {'id': vainqueur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vainqueur.id) }}">
    <button class="btn">Delete</button>
</form>
