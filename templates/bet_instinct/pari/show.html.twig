{% extends 'base.html.twig' %}

{% block title %}Pari{% endblock %}

{% block body %}
    <h1>Pari</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pari.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bet_instinct_pari_index') }}">back to list</a>

    <a href="{{ path('bet_instinct_pari_edit', {'id': pari.id}) }}">edit</a>

    {{ include('bet_instinct_pari/_delete_form.html.twig') }}
{% endblock %}
