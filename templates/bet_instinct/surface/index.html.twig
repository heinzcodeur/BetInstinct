{{include ('head.html.twig')}}
{% block body %}
    <h1>Surface index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for surface in surfaces %}
            <tr>
                <td>{{ surface.id }}</td>
                <td>{{ surface.name }}</td>
                <td>
                    <a href="{{ path('bet_instinct_surface_show', {'id': surface.id}) }}">show</a>
                    <a href="{{ path('bet_instinct_surface_edit', {'id': surface.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('bet_instinct_surface_new') }}">Create new</a>

    {{include ('head.html.twig')}}

{% endblock %}
