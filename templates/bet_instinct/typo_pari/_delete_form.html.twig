<form method="post" action="{{ path('bet_instinct_typo_pari_delete', {'id': typo_pari.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typo_pari.id) }}">
    <button class="btn">Delete</button>
</form>
