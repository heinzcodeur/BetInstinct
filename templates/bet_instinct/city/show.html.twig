{{include ('head.html.twig')}}

{% block body %}
    <h1>City</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ city.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bet_instinct_city_index') }}">back to list</a>

    <a href="{{ path('bet_instinct_city_edit', {'id': city.id}) }}">edit</a>


    {{ include('end.html.twig') }}
{% endblock %}
